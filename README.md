# Alpine Docker image with bash

This image is based on Alpine Linux image, which have a size only of 5MB image, and contains Bash (#!/bin/bash) interpreter.


## Usage

```bash
docker run --rm -ti ieperez/alpine-bash
```
```bash
bash-4.4#
```
